apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ntlg-app.fullname" . }}-test-connection"
  labels:
    {{- include "ntlg-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ntlg-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
